{
  "name": "couples-expense-tracker",
  "version": "1.0.0",
  "description": "A mobile-friendly expense tracker for couples",
  "homepage": "https://Bkrowchuk.github.io/couples-expense-tracker",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "deploy:firebase": "bash deploy.sh",
    "deploy:gh-pages": "npm run build && gh-pages -d dist",
    "deploy:gh-pages-script": "bash deploy-gh-pages.sh",
    "predeploy": "npm run build"
  },
  "dependencies": {
    "@vueuse/core": "^10.7.0",
    "firebase": "^10.14.1",
    "vue": "^3.3.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.0",
    "autoprefixer": "^10.4.0",
    "firebase-tools": "^13.0.0",
    "gh-pages": "^6.3.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.3.0",
    "vite": "^5.0.0"
  }
}
